name: Scheduled to run every 2 min

run-name: Scheduled Run

on:
  schedule:
  - cron: '*/2 * * * *'

jobs:
  show1:
    runs-on: ubuntu-latest
    steps:
    - name: Print time and date
      run: echo "The current date and time $(date)"
